<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true"
      internalLogLevel="Info"
      internalLogFile="logs/internal-nlog.txt">

  <!-- Enable ASP.NET Core layout renderers -->
  <extensions>
    <add assembly="NLog.Web.AspNetCore"/>
  </extensions>

  <!-- Define targets where logs will be written -->
  <targets>
    <!-- Write all logs to file with correlation ID -->
    <target xsi:type="File" name="allfile" fileName="logs/aesir-all-${shortdate}.log"
            layout="${longdate}|${event-properties:item=CorrelationId}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}"
            archiveFileName="logs/archives/aesir-all-${shortdate}.{#}.log"
            archiveEvery="Day"
            archiveNumbering="Rolling"
            maxArchiveFiles="30"
            concurrentWrites="true"
            keepFileOpen="false"
            encoding="utf-8" />

    <!-- Write error logs to separate file with correlation ID -->
    <target xsi:type="File" name="errorfile" fileName="logs/aesir-error-${shortdate}.log"
            layout="${longdate}|${event-properties:item=CorrelationId}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}|url: ${aspnet-request-url}|action: ${aspnet-mvc-action}"
            archiveFileName="logs/archives/aesir-error-${shortdate}.{#}.log"
            archiveEvery="Day"
            archiveNumbering="Rolling"
            maxArchiveFiles="90"
            concurrentWrites="true"
            keepFileOpen="false"
            encoding="utf-8" />

    <!-- Console output for development with colors -->
    <target xsi:type="ColoredConsole" name="console"
            layout="${longdate}|${event-properties:item=CorrelationId}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}">
      <highlight-row condition="level == LogLevel.Debug" foregroundColor="Gray" />
      <highlight-row condition="level == LogLevel.Info" foregroundColor="White" />
      <highlight-row condition="level == LogLevel.Warn" foregroundColor="Yellow" />
      <highlight-row condition="level == LogLevel.Error" foregroundColor="Red" />
      <highlight-row condition="level == LogLevel.Fatal" foregroundColor="Red" backgroundColor="White" />
    </target>
  </targets>

  <!-- Rules to map loggers to targets -->
  <rules>
    <!-- Skip noisy framework logs -->
    <logger name="Microsoft.*" maxlevel="Info" final="true" />
    <logger name="System.Net.Http.*" maxlevel="Info" final="true" />
    <logger name="Microsoft.AspNetCore.SignalR.*" minlevel="Debug" writeTo="console" final="true" />

    <!-- All logs (Trace and above) go to all-file -->
    <logger name="*" minlevel="Trace" writeTo="allfile" />

    <!-- Error logs (Warn and above) go to error-file -->
    <logger name="*" minlevel="Warn" writeTo="errorfile" />

    <!-- ============================================== -->
    <!-- RESEARCH MODULE DEBUGGING (verbose output)    -->
    <!-- ============================================== -->
    <logger name="Aesir.Modules.Research.*" minlevel="Debug" writeTo="console" final="true" />

    <!-- Inference modules at Debug level (needed for chat service resolution) -->
    <logger name="Aesir.Modules.Inference.*" minlevel="Debug" writeTo="console" final="true" />

    <!-- Configuration service at Debug level (needed for agent loading) -->
    <logger name="Aesir.Modules.Configuration.*" minlevel="Debug" writeTo="console" final="true" />

    <!-- Infrastructure services at Debug level (needed for IChatService) -->
    <logger name="Aesir.Infrastructure.*" minlevel="Debug" writeTo="console" final="true" />

    <!-- All other modules - only warnings and errors to console -->
    <logger name="Aesir.*" minlevel="Warn" writeTo="console" final="true" />

    <!-- Default: only warnings and errors -->
    <logger name="*" minlevel="Warn" writeTo="console" />
  </rules>
</nlog>
