@page "/settings/inference-engines"
@layout Aesir.Client.Web.Modules.Chat.Layout.ChatLayout
@inject NavigationManager NavigationManager

@* Redirect to unified settings page with tab parameter *@

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/settings?tab=inference-engines", replace: true);
    }
}
