@page "/settings/mcp-servers"
@layout ChatLayout
@inject NavigationManager NavigationManager

@* Redirect to unified settings page with tab parameter *@

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/settings?tab=mcp-servers", replace: true);
    }
}
