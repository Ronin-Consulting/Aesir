@page "/settings/observability"
@namespace Aesir.Client.Web.Modules.Observability.Pages
@inject NavigationManager NavigationManager

@* Redirect to unified settings page with tab parameter *@

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/settings?tab=observability", replace: true);
    }
}
